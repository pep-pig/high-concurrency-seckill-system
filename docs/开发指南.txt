前端：
    1.根据需求设计交互逻辑
    2.设计页面
    3.负责设计URL以及每个URL需要获取哪些java对象到jsp中
    4.负责webapp目下的内容编写
后端：
    根据前端提供的URL及其要得到的对象，从数据库或者其他方式，通过java代码生成前端URL需要的对象，并放入到jsp中

    前端和后端就是通过给定的URL和其要求得到的java对象进行分工合作，互不影响，前端不管后端怎么得到这些java对象，后端只管给前端这些java对象
而不关系前端的人用这些对象去写什么

    采用敏捷开发的方式不断的迭代需求，持续交付。